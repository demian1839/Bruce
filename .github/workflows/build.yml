name: Build CrowPanel 7.0, CYD-2USB & JC-ESP32P4-M3 Firmware

on:
  push:
    branches:
      - "main"
      - "master"
      - "add-elecrow-7-board-7334288356036986098"
  pull_request:
    branches:
      - "main"
      - "master"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Python 3.11 einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: PlatformIO und Tools installieren
        run: |
          python -m pip install --upgrade pip
          pip install platformio esptool

      - name: PlatformIO Core & Packages cachen
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            .pio
          key: ${{ runner.os }}-pio-${{ hashFiles('**/platformio.ini', '**/boards/crowpanel-70.ini', '**/boards/CYD-2432S028/CYD-2432S028.ini', '**/boards/jc-esp32p4-m3/jc-esp32p4-m3.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-

      - name: Firmware kompilieren (CrowPanel 7.0 & CYD-2USB)
        run: |
          pio run -e crowpanel-70 -e CYD-2USB

      - name: JC-ESP32P4-M3 kompilieren (Headless WebUI)
        continue-on-error: true
        run: |
          pio run -e jc-esp32p4-m3

      - name: Binaries zu Flash-Images zusammenfuegen (Merged Bin)
        run: |
          mkdir -p .pio/build/crowpanel-70 .pio/build/CYD-2USB .pio/build/jc-esp32p4-m3
          # CrowPanel 7.0 (ESP32-S3: bootloader @ 0x0)
          if [ -f "Bruce-crowpanel-70.bin" ]; then
            cp Bruce-crowpanel-70.bin .pio/build/crowpanel-70/bruce_crowpanel70_merged.bin
          elif [ -d ".pio/build/crowpanel-70" ]; then
            python -m esptool --chip esp32s3 merge_bin \
              -o .pio/build/crowpanel-70/bruce_crowpanel70_merged.bin \
              --flash_mode dio \
              --flash_size 4MB \
              0x0 .pio/build/crowpanel-70/bootloader.bin \
              0x8000 .pio/build/crowpanel-70/partitions.bin \
              0x10000 .pio/build/crowpanel-70/firmware.bin
          fi

          # CYD-2USB (ESP32: bootloader @ 0x1000)
          if [ -f "Bruce-CYD-2USB.bin" ]; then
            cp Bruce-CYD-2USB.bin .pio/build/CYD-2USB/bruce_CYD2USB_merged.bin
          elif [ -d ".pio/build/CYD-2USB" ]; then
            python -m esptool --chip esp32 merge_bin \
              -o .pio/build/CYD-2USB/bruce_CYD2USB_merged.bin \
              --flash_mode dio \
              --flash_size 4MB \
              0x1000 .pio/build/CYD-2USB/bootloader.bin \
              0x8000 .pio/build/CYD-2USB/partitions.bin \
              0x10000 .pio/build/CYD-2USB/firmware.bin
          fi

          # JC-ESP32P4-M3 (ESP32-P4: bootloader @ 0x0, 16MB)
          if [ -f "Bruce-jc-esp32p4-m3.bin" ]; then
            cp Bruce-jc-esp32p4-m3.bin .pio/build/jc-esp32p4-m3/bruce_JC-ESP32P4-M3_merged.bin
          elif [ -d ".pio/build/jc-esp32p4-m3" ]; then
            python -m esptool --chip esp32p4 merge_bin \
              -o .pio/build/jc-esp32p4-m3/bruce_JC-ESP32P4-M3_merged.bin \
              --flash_mode qio \
              --flash_size 16MB \
              0x0 .pio/build/jc-esp32p4-m3/bootloader.bin \
              0x8000 .pio/build/jc-esp32p4-m3/partitions.bin \
              0x10000 .pio/build/jc-esp32p4-m3/firmware.bin || true
          fi

      - name: CrowPanel 7.0 Artefakt hochladen
        uses: actions/upload-artifact@v4
        with:
          name: bruce-crowpanel70-firmware
          path: |
            .pio/build/crowpanel-70/firmware.bin
            .pio/build/crowpanel-70/bootloader.bin
            .pio/build/crowpanel-70/partitions.bin
            .pio/build/crowpanel-70/bruce_crowpanel70_merged.bin
          retention-days: 14

      - name: CYD-2USB Artefakt hochladen
        uses: actions/upload-artifact@v4
        with:
          name: bruce-CYD-2USB-firmware
          path: |
            .pio/build/CYD-2USB/firmware.bin
            .pio/build/CYD-2USB/bootloader.bin
            .pio/build/CYD-2USB/partitions.bin
            .pio/build/CYD-2USB/bruce_CYD2USB_merged.bin
          retention-days: 14

      - name: JC-ESP32P4-M3 Artefakt hochladen
        uses: actions/upload-artifact@v4
        with:
          name: bruce-JC-ESP32P4-M3-firmware
          path: |
            .pio/build/jc-esp32p4-m3/firmware.bin
            .pio/build/jc-esp32p4-m3/bootloader.bin
            .pio/build/jc-esp32p4-m3/partitions.bin
            .pio/build/jc-esp32p4-m3/bruce_JC-ESP32P4-M3_merged.bin
          retention-days: 14
